##KeyFix

## 💡 Огляд та Унікальна Цінність (UVP)

KeyFix – це кросплатформовий десктопний додаток, розроблений для миттєвої корекції тексту, набраного на неправильній розкладці. Його головна функція — виправлення тексту **на вимогу** користувача за допомогою глобальної гарячої клавіші.

**Унікальна Цінність (UVP):** Наш додаток надає користувачеві **повний контроль**, активуючись **виключно на вимогу** (Hotkey-triggered), що усуває проблему небажаного втручання, притаманну автоматичним аналогам.

* **Надійне Ядро:** Логіка конвертації працює **офлайн**, використовуючи модульні JSON-словники.
* **Інтелектуальний Шар:** Інтеграція **Gemini AI** для контекстної технічної підтримки.
* **Сучасний UX:** Кастомізація Hotkey та підтримка Світлої/Темної теми.

---

## 🚀 Як Користуватися (Для Кінцевого Користувача)

Після встановлення програми на ваш комп'ютер:

1.  **Запуск:** Додаток автоматично переходить у **фоновий режим** і з'являється в системному треї.
2.  **Дія:** У будь-якій програмі (браузер, редактор, месенджер) виділіть текст, набраний на неправильній розкладці (наприклад, `ghbdsn`).
3.  **Корекція:** Натисніть гарячу клавішу: **$\mathbf{Ctrl + Alt + R}$** (за замовчуванням).
4.  **Результат:** Текст миттєво конвертується у правильну мову (`привіт`).

### ⚙️ Налаштування

Щоб змінити параметри (тему, Hotkey, мови), відкрийте вікно KeyFix через системний трей. Усі зміни зберігаються автоматично.

---

## 🛠️ Для Розробників та Технічного Аналізу

### Стек Технологій

| Компонент | Технологія | Роль у Проєкті |
| :--- | :--- | :--- |
| **GUI** | **PyQt/PySide** | Створення нативного інтерфейсу та управління QSS (теми). |
| **Система** | **`pynput`** | Глобальне перехоплення Hotkey та імітація $\text{Ctrl}+\text{C}/\text{Ctrl}+\text{V}$. |
| **Буфер** | **`pyperclip`** | Безпечна робота з буфером обміну для отримання/вставки тексту. |
| **Конфігурація** | **JSON/Python OOP** | Модульне зберігання налаштувань та гнучке завантаження мовних словників. |

### Початкове Встановлення (Для Розробки)

**Кінцевому користувачу виконувати ці кроки не потрібно!**

1.  **Клонування Репозиторію:**
    ```bash
    git clone [https://github.com/](https://github.com/)
    cd KeyFix
    ```
2.  **Створення Віртуального Середовища та Залежностей:**
    ```bash
    python -m venv venv
    source venv/bin/activate # macOS/Linux
    venv\Scripts\activate    # Windows
    pip install pyqt6 pynput pyperclip google-genai
    ```
3.  **Налаштування AI (Опціонально):** Для тестування функції підтримки створіть файл **`.env`** у корені проєкту та додайте ваш API-ключ Gemini:
    ```
    GEMINI_API_KEY="ВАШ_КЛЮЧ"
    ```

### Структура Git

Проєкт використовує гілкову стратегію, засновану на GitFlow:

* **`main`:** Містить лише **стабільний, релізний** код.
* **`develop`:** Гілка інтеграції, куди зливаються всі нові функції.
* **`feature/*`:** Гілки для ізольованої розробки (наприклад, `feature/hotkey-listener`, `feature/ai-integration`).

---
